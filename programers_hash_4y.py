def solution(g, p):
    #print(g)
    #print(p)
    all_m={}
    for i in range(0,len(p)):
        #all_m.insert(0,[p[i]])
        all_m.insert

"""
    for i in range(0,len(g)):
        all_m[p[i]] = all_m[p[i]]+p[i]
        print(p[i])
        """



    answer = []

    return all_m


g=['classic', 'pop', 'classic', 'classic', 'pop']
p=[500, 600, 150, 800, 2500]

print(solution(g,p))