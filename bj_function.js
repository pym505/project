//const input = require('fs').readFileSync('dev/stdin').toString().split('\n');

//const input = [1, 3, 5, 7, 9, 20, 31, 42, 53, 64, 75, 86, 97];



/*
let arr = [];
main();
console.log(arr.join('\n'));

function main() {
    for (let i = 1; i <= 10000; i++) {
        arr.push(i);
        //    sss(i);
    }
    for (let i = 1; i <= 10000; i++) {
        sss(i);
        //break;
    }
    //console.log("aaaaa");
}

function sss(num) {

    while (num <= 10000) {
        let snum = String(num);
        for (let i = 0; i < snum.length; i++) {
            num += parseInt(snum[i])
        }
        let target = arr.indexOf(num);
        //console.log("target = " + target + ",num=" + num + ",arr[]" + arr[target]);
        if (target !== -1) arr.splice(target, 1);
        //console.log(num);
    }
}
*/