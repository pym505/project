// 씨 언어 다시 처음부터 처음부터 시작해보자. 
// 블로그에다가 직접 작성하기엔 내 PC 화면이 작다. 
// 작업환경은 vs code + code_runner + mingw gcc 컴파일러
// vs code 설치 후, mingw + 컴파일러 + path 설정 + code runner 다운
// 그 후 code runner 설치 후 작성 후 run이다.
#include <stdio.h>
int main()
{
    printf("aaaaaaa");
}